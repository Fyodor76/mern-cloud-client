import {styled} from '@mui/material/styles';

export const MainPageBlock = styled('div')({
  width: '80%',
  margin: '50px auto',
  backgroundColor: 'white',
  height: '80vh',
  borderRadius: '10px',
});

export const BlockFiles = styled('div')({
  padding: '5px',
});